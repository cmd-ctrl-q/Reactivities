using System;

namespace Domain
{
    public class Value
    {
        // concise way of declaring a property when no logic is required in the property accesses (getter and setter)
        public int Id { get; set; }
        public string Name{ get; set; }
    }
}
